[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量请在 Netlify 控制台配置 (Site Settings → Environment Variables)
# VITE_SUPABASE_URL = 你的 Supabase Project URL
# VITE_SUPABASE_ANON_KEY = 你的 Supabase anon key
